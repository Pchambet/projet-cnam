# Scripts obsolètes — protocole « stabilité » ad hoc

Ces fichiers ne font plus partie du pipeline de l’expérience 1. L’expérience est
recentrée sur **l’instabilité bootstrap** (Fang & Wang, 2012), via
`fpc::nselectboot` (`run_all_nselectboot.R`, `analyse_nselectboot.R`,
`generate_confusion_nselectboot.R`).

Conservés ici uniquement pour référence historique :

- `run_stabilite.R`, `run_all_stability.R`, `analyse_stabilite.R`
- `generate_confusion.R` (confusion à partir de `stabilite_*_best.csv`)
- `generate_confusion_both.R` (stabilité + nselectboot)
